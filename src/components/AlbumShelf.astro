---
import { albums } from '../data/collection';
import MediaShelf from './MediaShelf.astro';
const items = albums.map(album => ({ ...album, subtitle:album.artist, cover:`/music/${album.id}.webp`, width:160, height:160 }));
---
<MediaShelf kind="record" label="Favorite albums" items={items}/>
